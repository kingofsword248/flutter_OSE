import 'package:flutter/material.dart';

class WaitingTap extends StatelessWidget {
  const WaitingTap({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("fick"),
      ),
    );
  }
}
